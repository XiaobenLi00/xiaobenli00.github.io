# Project Pages

Place standalone project websites under this folder to keep the repository root clean.

Recommended layout:

- `projects/etch-x/` for ETCH-X static files
- `projects/<project-slug>/` for other project sites

If you want a short public URL like `/ETCH-X/`, create a page in `_pages/` with a permalink and redirect it to the nested folder.
